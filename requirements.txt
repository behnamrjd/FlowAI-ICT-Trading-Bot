# Core Telegram Bot
python-telegram-bot==13.15
urllib3==1.26.18 # Pinned for stability, as seen in Install.sh

# Data Processing & Analysis
pandas~=1.5.0 # Compatible release with 1.5
numpy~=1.21.0 # Compatible release with 1.21
requests~=2.28.0 # Compatible release with 2.28
joblib~=1.1.0 # For saving/loading AI models or other objects

# Configuration & Environment
python-dotenv~=0.19.0 # Compatible release with 0.19

# Technical Analysis
ta==0.10.2 # Standardized TA library, easier to install
TA-Lib~=0.4.24 # Requires TA-Lib C library to be installed separately

# Scheduling
schedule~=1.1.0 # For scheduling tasks

# Date & Time Handling
# jdatetime~=4.1.0 # Compatible release with 4.1
# pytz~=2022.1 # Compatible release with 2022.1

# Async Support
# asyncio-mqtt>=0.11.1 # Retaining as commented, appears optional
# aiohttp~=3.8.0 # Compatible release with 3.8 (potentially used by python-telegram-bot)
# apscheduler<4.0.0 # Pinned for stability, as seen in Install.sh (replaced by schedule)

# Logging & Monitoring
# colorlog~=6.6.0 # Compatible release with 6.6

# System Monitoring (Optional)
psutil~=5.9.0 # Compatible release with 5.9

# Other dependencies from Install.sh for completeness
six # From Install.sh
certifi # From Install.sh
cryptography # From Install.sh

# Database Support (Optional)
# sqlalchemy>=1.4.0

# Advanced Features (Optional)
# matplotlib>=3.5.0
# plotly>=5.10.0
# redis>=4.3.0
# scikit-learn~=1.1.0 # For AI/ML features

# Development Tools (Optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0
